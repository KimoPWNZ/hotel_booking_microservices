# Hotel Booking Microservices

Микросервисная система управления бронированием отелей на Java 17, Spring Boot 3.3.6, Spring Cloud, JWT, с использованием паттерна Saga для координации транзакций.

## 🏗️ Архитектура

```
┌─────────────────┐
│   API Gateway   │ :8080 (JWT validation, routing, correlation ID)
│   (Spring GW)   │
└────────┬────────┘
         │
         ├─────────────┬──────────────┬──────────────┐
         │             │              │              │
    ┌────▼────┐   ┌───▼───────┐ ┌───▼──────────┐ ┌─▼────────┐
    │ Eureka  │   │  Hotel    │ │   Booking    │ │  Users   │
    │ Server  │   │  Service  │ │   Service    │ │  (Auth)  │
    └─────────┘   └───────────┘ └──────────────┘ └──────────┘
      :8761         (H2 DB)        (H2 DB)         (JWT)
                   Rooms/Locks     Bookings
```

### Компоненты

- **eureka-server** — Service Discovery (Netflix Eureka) на порту 8761
- **api-gateway** — Маршрутизация запросов, JWT проброс, correlation ID, на порту 8080
- **hotel-service** — Управление отелями, номерами, блокировками (Saga participant)
- **booking-service** — Аутентификация (JWT), бронирования, Saga orchestration

### База данных

- **H2 In-Memory** для обоих сервисов (легко заменить на PostgreSQL)
- **Flyway** миграции для версионирования схемы БД
- **Индексы** на критичных полях для оптимизации запросов

## 🚀 Запуск

### Предварительные требования

- Java 17+
- Maven 3.6+

### Сборка и запуск

```bash
# Сборка всех модулей
mvn clean package -DskipTests

# В отдельных терминалах:
# 1. Eureka Server
cd eureka-server && mvn spring-boot:run

# 2. API Gateway (подождать пока Eureka запустится)
cd api-gateway && mvn spring-boot:run

# 3. Hotel Service
cd hotel-service && mvn spring-boot:run

# 4. Booking Service
cd booking-service && mvn spring-boot:run
```

### Переменные окружения

- `JWT_SECRET` — секретный ключ для JWT (по умолчанию: "devsecret")

## 📡 API Эндпоинты

### Через API Gateway: http://localhost:8080

#### Аутентификация (Booking Service)

```bash
# Регистрация
POST /user/register
{
  "username": "user",
  "password": "password",
  "role": "ROLE_USER"
}

# Вход
POST /user/auth
{
  "username": "user",
  "password": "password"
}
# Ответ: {"token": "JWT_TOKEN"}
```

#### Бронирования (требуется JWT токен)

```bash
# Создать бронирование
POST /booking
Header: Authorization: Bearer {JWT_TOKEN}
Header: X-Request-Id: {UNIQUE_ID} (optional, для идемпотентности)
{
  "roomId": 1,
  "startDate": "2026-01-10",
  "endDate": "2026-01-15",
  "autoSelect": false
}

# Список бронирований (с пагинацией)
GET /bookings?page=0&size=20
Header: Authorization: Bearer {JWT_TOKEN}

# Детали бронирования
GET /booking/{id}
Header: Authorization: Bearer {JWT_TOKEN}

# Отмена бронирования
DELETE /booking/{id}
Header: Authorization: Bearer {JWT_TOKEN}
```

#### Отели (Hotel Service)

```bash
# Список отелей
GET /api/hotels
Header: Authorization: Bearer {JWT_TOKEN}

# Создать отель (ADMIN только)
POST /api/hotels
Header: Authorization: Bearer {JWT_TOKEN}
{
  "name": "Grand Hotel",
  "address": "Main St 123"
}

# Обновить отель (ADMIN)
PATCH /api/hotels/{id}

# Удалить отель (ADMIN)
DELETE /api/hotels/{id}
```

#### Номера

```bash
# Список доступных номеров
GET /api/rooms
Header: Authorization: Bearer {JWT_TOKEN}

# Рекомендации (сортировка по загруженности)
GET /api/rooms/recommend
Header: Authorization: Bearer {JWT_TOKEN}

# Статистика загруженности номера
GET /api/rooms/{id}/stats
Header: Authorization: Bearer {JWT_TOKEN}

# Создать номер (ADMIN)
POST /api/rooms
{
  "hotelId": 1,
  "number": "101"
}

# Обновить доступность (ADMIN)
PATCH /api/rooms/{id}
{
  "available": true
}

# Удалить номер (ADMIN)
DELETE /api/rooms/{id}
```

### Swagger UI

- Hotel Service: http://localhost:{random}/swagger-ui/index.html
- Booking Service: http://localhost:{random}/swagger-ui/index.html
- (порты динамические, смотри логи)

## 🎯 Ключевые возможности

### 1. Алгоритм выбора номеров

**Справедливая стратегия**: Номера сортируются по `times_booked ASC`, затем по `id ASC`
- Предотвращает "простаивание" номеров
- Обеспечивает равномерную загрузку
- Детерминированный tie-breaker для одинаковой загруженности

### 2. Saga Pattern (Orchestration)

**Двухэтапная транзакция**:
1. **Booking Service** создает бронирование в статусе PENDING
2. Вызывает **Hotel Service** для подтверждения доступности (confirm-availability)
3. При успехе: статус → CONFIRMED
4. При ошибке/таймауте: компенсация (release) → статус CANCELLED

**Гарантии**:
- Таймауты и retry (настраивается)
- Идемпотентность через `requestId`
- Атомарность через Spring `@Transactional`

### 3. Идемпотентность

- **X-Request-Id** заголовок для дедупликации запросов
- Таблица `processed_requests` хранит обработанные ID
- Повторный запрос возвращает тот же результат

### 4. Concurrency Control

- **Database-level locking** через unique constraints
- Проверка пересечений дат перед созданием блокировки
- Возврат 409 Conflict при коллизии

### 5. Security

- **JWT (HS256)** для аутентификации
- **Role-based access** (ROLE_USER, ROLE_ADMIN)
- **Method security** через `@PreAuthorize`
- Автоматическая валидация токенов в каждом сервисе

### 6. Observability

- **Correlation ID** (`X-Correlation-Id`) через все сервисы
- Структурированное логирование с MDC
- Actuator endpoints для health checks

### 7. Error Handling

- Единообразные `ErrorResponse` DTO
- Global `@RestControllerAdvice` в каждом сервисе
- Правильные HTTP статусы: 400 (validation), 409 (conflict), 404 (not found), 403 (forbidden)

## 🧪 Тестирование

```bash
# Все тесты
mvn test

# Отдельный модуль
mvn test -pl hotel-service

# Конкретный тест
mvn test -Dtest=RoomServiceTest#recommendSorted
```

### Покрытие тестов

- ✅ Saga confirmation success
- ✅ Saga compensation on failure  
- ✅ Идемпотентность повторных запросов
- ✅ Conflict detection при параллельных бронированиях
- ✅ Сортировка рекомендаций по загруженности
- ✅ Валидация входных данных (400 errors)
- ✅ Authorization checks (401/403)

## 🗄️ База данных

### Схема (упрощенно)

**Hotel Service:**
- `hotels` — отели
- `rooms` — номера (привязаны к отелям)
- `room_locks` — блокировки номеров на даты (Saga state)

**Booking Service:**
- `users` — пользователи + JWT
- `bookings` — бронирования
- `processed_requests` — идемпотентность

**Миграции**: `src/main/resources/db/migration/V*.sql`

### Seed Data

- 3 отеля, 10 номеров с разными `times_booked`
- 4 пользователя (1 admin + 3 users, password: "password")

## 📚 Архитектурные решения (ADR)

### ADR-1: Saga Orchestration vs Choreography

**Выбор**: Orchestration (Booking Service координирует)

**Причины**:
- Проще отслеживать состояние всей транзакции
- Централизованная логика компенсации
- Меньше связности между сервисами
- Упрощенная отладка

### ADR-2: Idempotency через X-Request-Id

**Выбор**: Клиент передает уникальный ID, сервер дедуплицирует

**Причины**:
- Защита от двойных списаний при retry
- Соответствие HTTP семантике (idempotent POST)
- Простая реализация через DB constraint

### ADR-3: JWT в каждом сервисе (vs. Shared Session)

**Выбор**: Stateless JWT валидация в каждом микросервисе

**Причины**:
- Отсутствие shared state (масштабируемость)
- Независимые деплои
- Меньше latency (без обращения к auth service)

### ADR-4: H2 In-Memory для DEV (Flyway ready для PROD)

**Выбор**: H2 для разработки/тестов, Flyway миграции для переноса на PostgreSQL

**Причины**:
- Быстрая итерация в разработке
- Версионирование схемы через миграции
- Легкий переход на production DB (только connection string)

### ADR-5: Fair room selection (least-used-first)

**Выбор**: Сортировка по `times_booked ASC, id ASC`

**Причины**:
- Равномерная амортизация использования
- Предотвращение "холодных" номеров
- Детерминированный порядок (важно для тестов)

## 🔧 Конфигурация

### Hotel Service (application.yml)

```yaml
spring:
  jpa:
    hibernate:
      ddl-auto: validate  # Flyway управляет схемой
  flyway:
    enabled: true
    baseline-on-migrate: true
```

### Booking Service (application.yml)

```yaml
booking:
  saga:
    hotel:
      timeout-ms: 2000
      retries: 3
      backoff-ms: 200
logging:
  pattern:
    console: '%d [%X{X-Correlation-Id:-}] %msg%n'
```

## 🐛 Troubleshooting

**Eureka not registering services**:
- Проверь что Eureka запущен первым
- Подожди 30-60 секунд для регистрации

**JWT validation fails**:
- Убедись что `JWT_SECRET` одинаковый во всех сервисах
- Проверь формат токена: `Bearer eyJ...`

**Database lock conflicts**:
- Нормальное поведение при конкурентных запросах
- Клиент должен обработать 409 Conflict

## 📈 Метрики и мониторинг

- `/actuator/health` — статус сервиса
- `/actuator/info` — информация о приложении
- Correlation ID прокидывается через все сервисы для distributed tracing

## 📝 License

MIT License

## 👤 Автор

Юдкин М.М.
